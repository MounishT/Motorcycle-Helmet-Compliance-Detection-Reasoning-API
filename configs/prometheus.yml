# Prometheus configuration for Helmet Detection API

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'helmet-detection-api'
    static_configs:
      - targets: ['helmet-detection-api:8000']
    metrics_path: '/metrics'
    scrape_interval: 10s
